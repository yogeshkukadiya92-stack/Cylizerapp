import{describe,expect,it,vi}from"vitest";import{PostgresEmailDeliveryQueue}from"../src/postgres/notification-delivery-repository.js";import type{PgPoolLike}from"../src/postgres/types.js";
describe("PostgresEmailDeliveryQueue",()=>{it("claims a bounded email delivery",async()=>{const pool={query:vi.fn().mockResolvedValue({rows:[{organization_id:"org",delivery_id:"delivery",user_id:"user",recipient_email:"owner@example.com",event_key:"export_ready",payload:{jobId:"job"},attempt_count:1}]}),connect:vi.fn(),end:vi.fn()}as unknown as PgPoolLike;await expect(new PostgresEmailDeliveryQueue(pool,"worker").claim("worker")).resolves.toMatchObject({id:"delivery",recipientEmail:"owner@example.com",attemptCount:1});});});
