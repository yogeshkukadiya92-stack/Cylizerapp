import assert from "node:assert/strict"; import test from "node:test"; import { readSchemaFiles } from "../scripts/schema-contract.mjs"; const sql=readSchemaFiles().schemaSql;
test("Phase 7 credentials are encrypted or hashed and deliveries are replay safe",()=>{assert.match(sql,/key_hash bytea not null/);assert.doesNotMatch(sql,/api_keys[\s\S]{0,300}secret text/i);assert.match(sql,/credentials_ciphertext bytea not null/);assert.match(sql,/unique\(organization_id,endpoint_id,event_id\)/i);assert.match(sql,/attempt_count between 0 and 8/i);});
test("billing events are replay safe and subscription limits fail closed",()=>{assert.match(sql,/unique\(provider_event_id\)/i);assert.match(sql,/subscription_status_check/);assert.match(sql,/seat_limit>0 and storage_limit_bytes>0 and api_requests_per_minute>0/i);});
