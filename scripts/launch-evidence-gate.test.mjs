import assert from "node:assert/strict"; import test from "node:test"; import { verifyLaunchEvidence } from "./launch-evidence-gate.mjs";
const valid={version:1,security:{openCritical:0,openHigh:0,independentReviewId:"SEC-1"},load:{peakRequestsPerSecond:50,sustainedRequestsPerSecond:100,errorRate:.001,p95Ms:250,targetP95Ms:500},restore:{evidenceId:"DR-1",actualRpoMinutes:5,targetRpoMinutes:15,actualRtoMinutes:20,targetRtoMinutes:60},pilots:[{normalUseDays:14,signOffId:"P1"},{normalUseDays:18,signOffId:"P2"}],privacy:{counselApprovalId:"L1",dpaApprovalId:"L2",subprocessorApprovalId:"L3"},stores:{playApproved:true,appStoreApproved:true},drills:{exportEvidenceId:"E",deletionEvidenceId:"D",incidentEvidenceId:"I",rollbackEvidenceId:"R"},owners:{product:"a",engineering:"b",security:"c",support:"d"}};
test("accepts complete launch evidence",()=>assert.deepEqual(verifyLaunchEvidence(valid),[])); test("fails closed for missing external proof",()=>{const errors=verifyLaunchEvidence({version:1});assert.ok(errors.length>=8);}); test("requires two-times load headroom",()=>{const evidence=structuredClone(valid);evidence.load.sustainedRequestsPerSecond=99;assert.match(verifyLaunchEvidence(evidence).join(" "),/2x headroom/);});
